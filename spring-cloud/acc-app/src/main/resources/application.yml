spring:
  application:
    name: acc-app
server:
  port: 9000
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
    register-with-eureka: true
    fetch-registry: true